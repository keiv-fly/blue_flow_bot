from setuptools import setup, find_packages

setup(
    name="blue_flow_bot",
    version="0.5.0",
    packages=find_packages(),
    install_requires=[
        # list your runtime deps here, e.g. "aiohttp>=3.8.0"
    ],
)
